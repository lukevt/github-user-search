import React from 'react'
import './styles.css'

const Header = () =>(
    <nav className="header-container">
        <h1 className="header-title">GithubUserFinder</h1>
    </nav>
);

export default Header;